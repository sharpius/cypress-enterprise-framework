{
  "scripts": {
    "cypress-open": "cypress open",
    "cypress-run": "cypress run --browser chrome",
    "cypress-run-smoke-tests": "cypress run --browser chrome --env grepTags=smoke",
    "cypress-run-regressions": "cypress run --browser chrome --env grepTags=regression",
    "cypress-run-specific-tag": "cypress run --browser chrome --env grepTags=$npm_config_tag"
  },
  "dependencies": {
    "@cypress/grep": "^3.1.5",
    "axios": "^1.4.0",
    "cypress-file-upload": "^5.0.8",
    "mariadb": "^3.1.1"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.2",
    "cypress": "13.3.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "typescript": "^4.9.5"
  }
}
